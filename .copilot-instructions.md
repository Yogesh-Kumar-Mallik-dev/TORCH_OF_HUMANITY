You are working in a repository that uses a STRICT commit and documentation system.

The file `.gitmessage` is the SINGLE SOURCE OF TRUTH for:
- Commit structure
- Rules
- Allowed prefixes and tags
- Documentation workflow

You MUST follow `.gitmessage` EXACTLY.


=========================================================
CORE INSTRUCTIONS
=========================================================

1. ALWAYS read and follow `.gitmessage` before generating:
   - commit messages
   - documentation
   - summaries of changes

2. DO NOT invent or modify the commit format
3. DO NOT skip any required section (especially Impact and Author)
4. DO NOT simplify or shorten the structure


=========================================================
STRICT ENFORCEMENT RULES
=========================================================

- Multiple commit blocks are ALLOWED and should be used for clarity
- Each block must represent ONE logical system/change
- Impact section is REQUIRED (never skip)
- Author must appear ONLY ONCE and at the END

- PREFIX = ACTION (what was done)
- TAG = CATEGORY (type of change)
- SCOPE = SYSTEM (where change happened)

TAG ≠ SCOPE


=========================================================
COMMIT USAGE RULE (ABSOLUTE)
=========================================================

- FULL `.gitmessage` structure is used ONLY for:
  - explicit commit requests
  - commit drafts

- DO NOT use full commit structure for:
  - normal chat responses
  - explanations
  - debugging

- NEVER generate a commit unless explicitly asked


=========================================================
DOCUMENTATION RULES (MANDATORY)
=========================================================

Before suggesting any commit:

1. Ensure documentation exists or is updated in `docs/`
2. Docs MUST mirror the SAME TREE STRUCTURE as the root project
3. Docs MUST mirror EVERY meaningful changed file:
   - Include: source code, systems, logic, config files
   - Exclude: images, audio, binaries, generated files
4. Every change must be reflected in documentation
5. If docs are missing → explicitly mention it


=========================================================
BRANCH RULE
=========================================================

- "Branch : <branch-name>" is REQUIRED in EVERY commit output
- MUST be derived from actual git state
- Use current branch (do not guess or hardcode)


=========================================================
MERGE RULE
=========================================================

- Use ONLY merge format:

  Branch : <branch-name>

  MERGE: (<source-branch>) --> {<target-branch>}

  Author: Yogesh-Kumar-Mallik-dev

- DO NOT include:
  - commit blocks
  - Impact section
  - extra explanations


=========================================================
AI THINKING RULE (CRITICAL)
=========================================================

Before generating output, think at least 3 times:

1. Is the change correctly understood?
2. Does it follow `.gitmessage` EXACTLY?
3. Is the Impact meaningful and not generic?

If unsure → ask instead of guessing.


=========================================================
OUTPUT RULE
=========================================================

When generating commits:
- Follow `.gitmessage` EXACTLY
- Use correct PREFIX and TAG from allowed list
- Use clear and specific SCOPE
- Include meaningful Impact
- Use multiple blocks if needed
- Keep it concise but descriptive


=========================================================
CONFLICT RESOLUTION RULE (HIGHEST PRIORITY)
=========================================================

If rules conflict:

1. USER INSTRUCTION (highest priority)
2. WORKFLOW RULES
3. `.gitmessage`
4. Copilot instructions (this file)

User instruction ALWAYS overrides everything.


=========================================================
FAILURE CONDITION
=========================================================

If your response does NOT follow `.gitmessage` exactly,
it is considered incorrect.

You must strictly adhere to the system.