# .copilot-instructions.md

Status: repository workflow instructions configured.

## Summary
- AI workflow expectations are defined for commit generation behavior.
- Documentation mirroring and branch/merge constraints are described.
- Merge output requirements now include structured `Merge Notes` (major features, key systems, architecture/behavior impact).
- Merge policy now explicitly preserves source branches after merge (no deletion).

## Impact
- Keeps AI-assisted commit and merge outputs aligned with stricter repository governance.
